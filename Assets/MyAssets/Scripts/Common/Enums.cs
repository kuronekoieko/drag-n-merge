public enum ScreenState
{
    INITIALIZE,
    START,
    GAME,
    RESULT,
}

public enum GameState
{
}

public enum ResultState
{
    PLAYING,
    WIN,
    LOSE,
}

public enum BlockState
{
    STOP,
    FALL,
    DRAG,
}